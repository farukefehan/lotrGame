from tkinter import Label
from PIL import Image, ImageTk



def start_avontuur_11(venster):
    from main import kill_all_children
    kill_all_children(venster)

    vraag = 1
    if vraag == 1 and antwoord == 1:
        vraag += 1
    if vraag == 1 and antwoord == 2:


    List_with_screens = [
        Dictionary_per_screen = {
        "Question": string(vraag),
        "Options": list_with_options = [
            Dict_option1 = {
            "Text": string for label.
            "Action": action
        for click event.

            }
            Dict_option2 = {
            "Text": string
            for label.
            "Action": action
            for click event.
            ~extra data goes after~
            }
    Dict_option3 = {
    "Text": string
    for label.
    "Action": action
    for click event.
    ~extra data goes after~
    }
    ]
    }
    ]
